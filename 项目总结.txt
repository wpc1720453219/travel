JDBC 步骤
1.注册驱动
2.获取数据库连接对象
3.定义sql语句
4.获取执行sql的对象
5.执行sql
6.释放对象资源

JDBC 控制事务
1.开启事务 setAutoCommit（boolean autoCommit）
2.提交事务 commit（）
3.回滚事务 rollback()

JDBC数据库连接池
1.C3P0
2.Droid


旅游网 总结
1.注册功能
   前端: 用户名，密码，邮箱，名字，电话号 js中正则表达式验证
     通过ajax回调函数异步提交，将数据到后端进行验证
2.邮件激活
   注册成功后发生邮件到用户邮箱，邮箱里有激活链接，点击链接跳转项目里，验证激活，激活成功会跳转登录页面
3.登录功能
   到登录页面，如果登录成功，将用户存储到session中
4.退出
   退出，销毁session，到登录页面
5.亮点:
  优化Servlet
  减少Servlet的数量，现在是一个功能一个Servlet，将其优化为一个模块一个Servlet，相当于在数据库中一张表对应一个Servlet，在Servlet中提供不同的方法，完成用户的请求。
    第20页
6.分类数据展示
    旅游的话，有各种类型的，国外游，团购，自由行，几天游...这些分类
	这些分类的信息，因为基本是不变的，所以，可以用Redis，存储，先到Redis查询，如果为空
	在从数据里查询，并储存到Redis里有序集合
7.在每一个分类下，显示所选哪个旅游地方商品
  /* 展示分页页码

            1.一共展示10个页码，能够达到前5后4的效果
            2.如果前边不够5个，后边补齐10个
            3.如果后边不足4个，前边补齐10个
	*/
	通过ajax回调函数，显示数据，为js格式添加到html里面显示

  
   
   
   

